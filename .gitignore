.codegraph/

# Local runtime state
*.db
*.db-shm
*.db-wal

# Local environment files
.env
.env.*
!.env.example

# Build and editor output
webhook-inspector
coverage.out
.DS_Store


local-testing/

# Frontend dependencies and build output. `frontend/dist` must exist at Go
# build time because the binary embeds it, so the directory is kept.
frontend/node_modules
frontend/dist/*
!frontend/dist/.gitkeep
